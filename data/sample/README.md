# Sample data

Bundled, clearly-labelled sample datasets live here. They are populated in
**Phase 2** (data providers) so the platform can run end-to-end without live
network access. No real financial data is fabricated; illustrative datasets
(e.g. precedent transactions) are flagged in-model via `is_sample=True`.
